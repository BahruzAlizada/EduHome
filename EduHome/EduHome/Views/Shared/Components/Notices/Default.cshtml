@model List<Notice>

@foreach(Notice notice in Model)
{
    <div class="single-notice-left mb-23 pb-20">
        <h4>@notice.CreatedTime.ToString("dd MMMM, yyyy")</h4>
        <p>@notice.Description</p>
    </div>
}