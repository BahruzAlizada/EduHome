@model List<Service>

@foreach (Service service in Model)
{
    <div class="col-md-4 col-sm-4 col-xs-12">
        <div class="single-service">
            <h3><a href="teacher.html">@service.Name</a></h3>
            <p>@service.Description </p>
        </div>
    </div>
}